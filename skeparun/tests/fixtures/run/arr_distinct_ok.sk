import io;
import arr;

fn main() -> Int {
  let a: [Int; 7] = [3, 1, 3, 2, 1, 2, 2];
  let d = arr.distinct(a);
  if (arr.len(d) == 3 && arr.first(d) == 3 && arr.last(d) == 2) {
    io.printString("arr-distinct-ok");
    return 0;
  }
  return 1;
}
