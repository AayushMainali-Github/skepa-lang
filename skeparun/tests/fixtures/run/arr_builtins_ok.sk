import arr;
import io;

fn main() -> Int {
  let a: [Int; 4] = [1, 2, 3, 2];
  let b: [Int; 2] = [9, 8];
  let c = a + b;
  if (arr.len(c) == 6 && !arr.isEmpty(c) && arr.contains(c, 8) && arr.indexOf(c, 2) == 1 && arr.count(c, 2) == 2 && arr.first(c) == 1 && arr.last(c) == 8) {
    io.printInt(1);
    return 0;
  }
  return 1;
}
