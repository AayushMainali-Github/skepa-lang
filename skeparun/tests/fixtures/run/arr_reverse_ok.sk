import io;
import arr;

fn main() -> Int {
  let a: [Int; 5] = [1, 2, 3, 4, 5];
  let r = arr.reverse(a);
  if (arr.first(r) == 5 && arr.last(r) == 1 && arr.len(r) == 5) {
    io.printString("reverse-ok");
    return 0;
  }
  return 1;
}
