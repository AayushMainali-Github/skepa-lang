import io;
import arr;

fn main() -> Int {
  let a: [Int; 5] = [4, 1, 3, 1, 2];
  let s = arr.sort(a);
  if (arr.first(s) == 1 && arr.last(s) == 4 && arr.len(s) == 5) {
    io.printString("arr-sort-ok");
    return 0;
  }
  return 1;
}
