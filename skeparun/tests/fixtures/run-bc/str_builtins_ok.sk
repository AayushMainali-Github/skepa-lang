import io;
import str;

fn main() -> Int {
  let s = "skepa";
  let idx = str.indexOf(s, "ep");
  let cut = str.slice(s, 1, 4);
  if (idx == 2 && cut == "kep" && !str.isEmpty(cut) && str.isEmpty("")) {
    io.printInt(1);
    return 0;
  }
  return 1;
}
