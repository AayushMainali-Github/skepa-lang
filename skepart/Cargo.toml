[package]
name = "skepart"
version = "0.12.0"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
rustls = "0.23"
rustls-native-certs = "0.8"

[dev-dependencies]
rcgen = "0.13"
